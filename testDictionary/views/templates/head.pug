mixin AnimalList(animals = [])
    h2 Animals
    ul
        each animal in animals
            li=`${animal.name} `
                em=`${animal.code}`
        else
            li No Animals today

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title=title
    body
        nav
            a(href='/') Home
            a(href='/about') About
            a(href='/contact') Contact